# Backend auth

**Purpose:** How auth is implemented internally (hashing, tokens, sessions).

**Not applicable to this project.** There is no backend, no user accounts, no sessions, and no credential storage. The site is fully public.

For how API clients would authenticate (also N/A here, but documented for completeness): see [../api/auth.md](../api/auth.md).
