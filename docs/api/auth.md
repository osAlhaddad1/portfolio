# API auth

**Purpose:** How API clients authenticate (headers, tokens, OAuth flow).

**Not applicable to this project.** There is no API and no concept of a client identity. The site is a public static SPA.

For internal auth (sessions, hashing) see [../backend/auth.md](../backend/auth.md), which is also N/A here.
